---
# Note: the docker_tag of a GPU/cuda image MUST have the word "gpu", and that of a CPU image must have the word "cpu"
gpu:
    docker_tag: "neuron-dev-image"
    cuda_version: "cu102"
cpu:
    docker_tag: "neuron-dev-image"
