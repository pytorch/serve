name: Test docker image tagging


on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  test-docker-image:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
       
      - name: Test custom tagging with gpu flag
        working-directory: docker
        run: ./test_image_build_tagging.sh
