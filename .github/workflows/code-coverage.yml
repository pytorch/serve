name: Code Coverage

on: push
concurrency:
  group: codecov-${{ github.workflow }}-${{ github.ref == 'refs/heads/master' && github.run_number || github.ref }}
  cancel-in-progress: true

jobs:
  codecov:
    runs-on: ubuntu-latest
    steps:
      - name: Setup Python 3.8
        uses: actions/setup-python@v2
        with:
          python-version: 3.8
          architecture: x64
      - name: Setup Java 11
        uses: actions/setup-java@v2
        with:
          distribution: 'zulu'
          java-version: '11'

      - name: Checkout TorchServe
        uses: actions/checkout@v2
      - name: Install dependencies
        run: |
          python ts_scripts/install_dependencies.py --environment=dev
      - name: Torchserve Sanity
        run: |
          python torchserve_sanity.py
      - name: Run Python Unit Tests
        run: |
          python -m pytest --cov-report=xml:coveragetsut.xml --cov=./ ts/tests/unit_tests/
          cd model-archiver
          python -m pytest --cov-report=xml:coveragemaut.xml --cov=./ model_archiver/tests/unit_tests/
          python -m pytest --cov-report=xml:coveragemaint.xml --cov=./ model_archiver/tests/integ_tests/
          cd ../workflow-archiver
          python -m pytest --cov-report=xml:coveragewaut.xml --cov=./ workflow_archiver/tests/unit_tests/
          python -m pytest --cov-report=xml:coveragewaint.xml --cov=./ workflow_archiver/tests/integ_tests/
          cd ..
      - name: mvn install
        run: cd serving-sdk/ && mvn clean install -q && cd ../
      - name: Install codecov
        run : |
          curl -Os https://uploader.codecov.io/latest/linux/codecov
          chmod +x codecov
          ./codecov
      - name: Upload coverage to Codecov (script)
        uses: ./
        with:
          files: ./coverage/script/coverage-final.json
          flags: script,${{ matrix.os }}
          name: codecov-script
          verbose: true
